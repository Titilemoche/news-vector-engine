# Variables d'environnement pour le projet news-vector-engine
# Copiez ce fichier en .env et remplacez les valeurs ci-dessous par vos propres clés

# Clé API pour Google Gemini (utilisée par pipeline/llm_client.py)
# Récupérez votre clé depuis Google AI Studio ou Google Cloud Console
GEMINI_API_KEY="YOUR_GOOGLE_GEMINI_API_KEY_HERE"
# Alternativement, la librairie google-generativeai peut utiliser GOOGLE_API_KEY

# Clé API pour OpenAI (utilisée par pipeline/vectorize_articles.py pour les embeddings)
# Récupérez votre clé depuis la plateforme OpenAI
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Configuration Qdrant (utilisée par pipeline/qdrant_utils.py)
# Pour une instance Qdrant locale (par défaut si les variables ci-dessous ne sont pas définies pour le cloud)
QDRANT_HOST="localhost"
QDRANT_PORT=6333

# Pour Qdrant Cloud (décommentez et adaptez si vous utilisez Qdrant Cloud)
# QDRANT_HOST="YOUR_QDRANT_CLUSTER_URL_HERE"
# QDRANT_PORT=6333 # Ou 6334 pour gRPC/HTTPS si applicable pour votre cluster cloud
# QDRANT_API_KEY="YOUR_QDRANT_API_KEY_HERE" # Nécessaire si l'authentification est activée sur votre cluster Qdrant Cloud